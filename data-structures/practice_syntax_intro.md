# Практическое задание: Динамический массив и стек на Python

**Тема:** Реализация динамического массива и стека своими руками

**Цель:** Понять, как устроены базовые структуры данных изнутри, реализовав их на Python.

**Оборудование:** ОС РОСА Линукс, терминал, Python 3.8 (уже установлен).

---

## Часть 1. Динамический массив

### Что это такое

**Динамический массив** — это список, который умеет **сам расширяться**, когда в него добавляют новые элементы. В Python есть встроенный `list`, который работает именно так.

Внутри `list` устроен так: у него есть **обычный массив** фиксированного размера (capacity — «ёмкость») и **счётчик занятых ячеек** (size — «размер»). Когда места заканчивается, Python создаёт новый массив в 2 раза больше и копирует туда все элементы.

### Задание 1.1. Реализовать свой динамический массив

Создайте файл `my_array.py`:

```bash
cd ~/python_urok
nano my_array.py
```

Введите код:

```python
#!/usr/bin/env python3
# Мой динамический массив

class MyArray:
    def __init__(self):
        """Создаём пустой массив с начальной ёмкостью 2."""
        self.capacity = 2           # сколько всего ячеек
        self.size = 0               # сколько занято
        self.data = [None] * self.capacity

    def append(self, value):
        """Добавить элемент в конец."""
        if self.size == self.capacity:
            self._resize()
        self.data[self.size] = value
        self.size += 1

    def _resize(self):
        """Увеличить массив в 2 раза."""
        print(f"  [расширение: {self.capacity} -> {self.capacity * 2}]")
        self.capacity *= 2
        new_data = [None] * self.capacity
        for i in range(self.size):
            new_data[i] = self.data[i]
        self.data = new_data

    def get(self, index):
        """Получить элемент по индексу."""
        if index < 0 or index >= self.size:
            raise IndexError("Индекс вне диапазона")
        return self.data[index]

    def set(self, index, value):
        """Изменить элемент по индексу."""
        if index < 0 or index >= self.size:
            raise IndexError("Индекс вне диапазона")
        self.data[index] = value

    def length(self):
        """Сколько элементов в массиве."""
        return self.size

    def __str__(self):
        """Красивый вывод для print()."""
        elements = [str(self.data[i]) for i in range(self.size)]
        return "[" + ", ".join(elements) + "]"


# ---------- Проверка ----------
print("=== Динамический массив ===")

arr = MyArray()
print("Создали пустой массив:", arr)
print("Размер:", arr.length())
print()

# Добавляем элементы и следим за расширением
for i in range(1, 6):
    print(f"append({i})")
    arr.append(i)
    print("  Массив:", arr)
    print("  size =", arr.size, ", capacity =", arr.capacity)
    print()

# Доступ по индексу
print("Элемент с индексом 2:", arr.get(2))
arr.set(2, 100)
print("После set(2, 100):", arr)
```

**Запуск:**

```bash
python3 my_array.py
```

**Ожидаемый результат:**

```
=== Динамический массив ===
Создали пустой массив: []
Размер: 0

append(1)
  Массив: [1]
  size = 1 , capacity = 2

append(2)
  Массив: [1, 2]
  size = 2 , capacity = 2

append(3)
  [расширение: 2 -> 4]
  Массив: [1, 2, 3]
  size = 3 , capacity = 4
...
```

### Задание 1.2. Ответить на вопросы

1. Сколько раз произошло расширение при добавлении чисел от 1 до 5?
2. Что произойдёт, если убрать проверку `if self.size == self.capacity`?
3. Почему новый массив создаётся **в 2 раза** больше, а не на 1 ячейку?

### Задание 1.3. Усложнить (по желанию)

Добавьте метод `pop()` — удаление последнего элемента:

```python
def pop(self):
    """Удалить последний элемент и вернуть его."""
    if self.size == 0:
        return None
    value = self.data[self.size - 1]
    self.data[self.size - 1] = None
    self.size -= 1
    return value
```

Проверьте: добавьте 5 элементов, удалите 2, посмотрите результат.

---

## Часть 2. Стек

### Что это такое

**Стек** — структура данных, работающая по принципу **LIFO** (Last In, First Out): «последним пришёл — первым вышел».

**Аналогия:** стопка тарелок. Кладёшь сверху, берёшь сверху. До нижней тарелки доберёшься только тогда, когда снимешь все верхние.

**Три главные операции:**
- `push(x)` — положить на вершину
- `pop()` — снять с вершины
- `peek()` — посмотреть верхний элемент, не снимая

### Задание 2.1. Реализовать стек

Создайте файл `my_stack.py`:

```bash
nano my_stack.py
```

Введите код:

```python
#!/usr/bin/env python3
# Мой стек

class Stack:
    def __init__(self):
        """Создаём пустой стек."""
        self.items = []

    def push(self, value):
        """Положить элемент на вершину."""
        self.items.append(value)

    def pop(self):
        """Снять верхний элемент и вернуть его."""
        if self.is_empty():
            return None
        return self.items.pop()

    def peek(self):
        """Посмотреть верхний элемент, не снимая."""
        if self.is_empty():
            return None
        return self.items[-1]

    def is_empty(self):
        """Проверить, пуст ли стек."""
        return len(self.items) == 0

    def size(self):
        """Сколько элементов в стеке."""
        return len(self.items)

    def __str__(self):
        """Красивый вывод. Вершина стека — справа."""
        return "Стек: " + str(self.items) + " <- вершина"


# ---------- Проверка ----------
print("=== Стек ===")

s = Stack()
print("Создали пустой стек. is_empty() =", s.is_empty())
print()

# Кладём элементы
s.push("Первый")
s.push("Второй")
s.push("Третий")
print(s)
print("Размер:", s.size())
print("Верхний элемент:", s.peek())
print()

# Снимаем элементы
print("pop():", s.pop())
print("pop():", s.pop())
print("После двух pop:", s)
print("Размер:", s.size())
print()

# Достаём последний
print("pop():", s.pop())
print("pop() с пустого стека:", s.pop())
print("is_empty() =", s.is_empty())
```

**Запуск:**

```bash
python3 my_stack.py
```

**Ожидаемый результат:**

```
=== Стек ===
Создали пустой стек. is_empty() = True

Стек: ['Первый', 'Второй', 'Третий'] <- вершина
Размер: 3
Верхний элемент: Третий

pop(): Третий
pop(): Второй
После двух pop: Стек: ['Первый'] <- вершина
Размер: 1

pop(): Первый
pop() с пустого стека: None
is_empty() = True
```

### Задание 2.2. Ответить на вопросы

1. Почему в примере первым снимается «Третий», а не «Первый»?
2. Что вернёт `peek()` для пустого стека? Что произойдёт, если убрать проверку `is_empty`?
3. Чем `pop()` отличается от `peek()`?

### Задание 2.3. Придумать применение

Стек используется в реальной жизни. Приведите **три примера** из повседневности или программирования, где работает принцип LIFO:

- Пример из жизни: стопка тарелок.
- Пример из программирования: кнопка «Отменить» (Ctrl+Z) в редакторе.
- Ваш вариант: ...

---

## Часть 3. Практическое применение стека

### Задание 3.1. Проверка скобок

Стек часто используют для проверки правильности скобок в выражениях. Идея: открывающая скобка кладётся в стек, закрывающая — снимает верхнюю. Если в конце стек пуст — всё правильно.

Создайте `brackets.py`:

```python
#!/usr/bin/env python3
# Проверка правильности скобок

class Stack:
    def __init__(self):
        self.items = []

    def push(self, value):
        self.items.append(value)

    def pop(self):
        if not self.items:
            return None
        return self.items.pop()

    def is_empty(self):
        return len(self.items) == 0


def check_brackets(text):
    """Возвращает True, если скобки расставлены правильно."""
    stack = Stack()

    for char in text:
        if char == "(":
            stack.push(char)
        elif char == ")":
            if stack.pop() is None:   # закрывающая без открывающей
                return False

    return stack.is_empty()            # все открывающие закрыты


# ---------- Проверка ----------
tests = [
    "(2 + 3) * 4",          # правильно
    "((a + b) * c)",        # правильно
    "(2 + 3)) * 4",         # лишняя закрывающая
    "((2 + 3) * 4",         # не хватает закрывающей
    "2 + 3 * 4",            # скобок нет — тоже правильно
]

for t in tests:
    result = "OK" if check_brackets(t) else "ОШИБКА"
    print(f"{result:8} | {t}")
```

**Запуск:**

```bash
python3 brackets.py
```

**Ожидаемый результат:**

```
OK       | (2 + 3) * 4
OK       | ((a + b) * c)
ОШИБКА   | (2 + 3)) * 4
ОШИБКА   | ((2 + 3) * 4
OK       | 2 + 3 * 4
```

### Задание 3.2. Проверка палиндрома

**Палиндром** — строка, которая читается одинаково в обе стороны: «шалаш», «топот», «level».

Напишите функцию `is_palindrome(text)`, использующую стек:

1. Положите все символы строки в стек.
2. Сравнивайте символы строки с символами, которые снимаете из стека.
3. Если все совпали — палиндром.

**Подсказка:**

```python
def is_palindrome(text):
    stack = Stack()
    for ch in text:
        stack.push(ch)
    for ch in text:
        if ch != stack.pop():
            return False
    return True
```

Проверьте на словах: `шалаш`, `топот`, `привет`, `level`, `12321`.

### Задание 3.3. История действий (по желанию)

Представьте текстовый редактор. Каждое действие пользователя кладётся в стек. Команда «Отменить» снимает последнее действие.

Напишите программу, которая:
1. Спрашивает у пользователя действия (через `input`).
2. Кладёт их в стек.
3. По команде `undo` — снимает последнее действие и выводит «Отменено: ...».
4. По команде `exit` — завершается.

---

## Итоговая таблица

| Структура | Принцип | Ключевые операции | Где применяется |
|---|---|---|---|
| **Динамический массив** | Элементы по индексу | `append`, `get`, `set`, `pop` | Везде, где нужен список |
| **Стек** | LIFO — последний вошёл, первый вышел | `push`, `pop`, `peek` | Отмена действий, проверка скобок, палиндромы |

---

## Контрольные вопросы

1. Зачем динамическому массиву **ёмкость** (`capacity`), если есть **размер** (`size`)?
2. Почему в динамическом массиве расширение делают **в 2 раза**, а не на одну ячейку?
3. Чем `pop()` в стеке отличается от `pop()` в списке Python?
4. Почему проверку скобок удобно делать именно стеком, а не очередью?
5. Приведите пример задачи, где нужен стек, и пример, где нужна очередь.

---

## Домашнее задание

1. Добавьте в класс `MyArray` методы:
   - `insert(index, value)` — вставка в середину,
   - `remove(value)` — удаление первого вхождения значения.

2. Реализуйте **очередь** на основе двух стеков. Подсказка: один стек для добавления, второй — для извлечения.

3. Придумайте свою задачу, где пригодится стек, и решите её.

---

**Отчёт должен содержать:** листинги файлов `my_array.py`, `my_stack.py`, `brackets.py`, скриншоты запуска в РОСА Линукс, ответы на контрольные вопросы.

**Совет:** не копируйте код слепо — набирайте руками и экспериментируйте. Меняйте числа, добавляйте элементы, ломайте код специально, чтобы понять, как он работает. Это лучший способ научиться. 🐍
